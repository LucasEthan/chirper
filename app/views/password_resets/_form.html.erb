<%= form_with(url: password_resets_path, scope: :password_reset, local: true) do |f| %>
  <%= f.label :email %>
  <%= f.email_field :email, placeholder: "name@example.com", autofocus: true, class: 'form-control' %>
  <%= f.submit "Send the reset instructions", class: "btn btn-primary" %>
<% end %>